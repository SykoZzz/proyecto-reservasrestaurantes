@model IEnumerable<Restaurante>

@{
    ViewData["Title"] = "Restaurantes";
}

<!-- Barra de navegación -->
<nav class="navbar navbar-expand-lg navbar-dark" style="background-color:#BA965A;">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/" style="color:white;">
            🍽️ Mesa Lista
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link fw-semibold active" asp-controller="Restaurantes" asp-action="Index">Restaurantes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-semibold" asp-controller="Reservas" asp-action="Index">Mis Reservas</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<!-- Fondo principal -->
<div style="background-color:#F5E9E3; min-height:100vh; padding-top:60px;">
    <div class="container py-5">
        <!-- Título -->
        <h2 class="fw-bold text-center mb-5" style="color:#3E2723;">
            🍽 Restaurantes <span style="color:#BA965A;">Disponibles</span>
        </h2>

        <!-- Grid de restaurantes -->
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card shadow-lg border-0 rounded-4 h-100 overflow-hidden">
                        <!-- Imagen -->
                        <img src="@item.ImagenUrl" 
                             class="card-img-top" 
                             alt="@item.Nombre" 
                             style="height:200px; object-fit:cover;">

                        <!-- Info -->
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title fw-bold mb-3" style="color:#3E2723;">@item.Nombre</h5>
                            <p class="card-text text-muted mb-4">
                                <strong>🍽 Tipo:</strong> @item.Tipo <br />
                                <strong>📍 Dirección:</strong> @item.Direccion - @item.Distrito <br />
                                <strong>💰 Precio Promedio:</strong> S/. @item.PrecioPromedio <br />
                                <strong>⭐ Rating:</strong> @item.Rating
                            </p>

                            <!-- Botón -->
                            <a href="@Url.Action("Details", "Restaurantes", new { id = item.Id })" 
                               class="btn mt-auto px-4 py-2 shadow-lg fw-semibold" 
                               style="background-color:#BA965A; color:white; border-radius:12px; transition:0.3s;"
                               onmouseover="this.style.backgroundColor='#8C6B3F'" 
                               onmouseout="this.style.backgroundColor='#BA965A'">
                                🔎 Ver Detalles
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
